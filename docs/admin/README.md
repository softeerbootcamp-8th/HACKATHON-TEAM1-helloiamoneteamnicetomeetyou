# 어드민 화면 스크린샷

부스 운영용 어드민(`/admin`)의 화면들입니다. PR 본문에서 참조하고 있어서,
파일 이름을 바꾸면 그쪽 링크가 깨집니다.

로컬에서 직접 보려면 백엔드를 띄우고 `http://localhost:8080/admin` 으로 들어갑니다.
계정은 기본값이 `admin` / `admin1234` 이고 환경변수로 덮을 수 있습니다.
